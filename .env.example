# Copy to .env for local development
# Leave empty to use Vite's local proxy (configured in vite.config.js → /api → staging)
VITE_API_BASE=

# For Vercel deployment, set this to the API URL:
# VITE_API_BASE=https://staging-api.lentilai.com
